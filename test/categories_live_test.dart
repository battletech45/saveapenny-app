import 'package:flutter_test/flutter_test.dart';

void main() {
  test(
    'live categories call against local backend',
    () {
      // Requires running local backend with valid tokens — not suitable for CI.
    },
    skip: 'Requires running local backend',
  );
}
