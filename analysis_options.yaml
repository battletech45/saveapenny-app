# Strict static analysis. This is pillar #2 of AI consistency:
# rules the model cannot silently drift past.

include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  errors:
    # Treat these as build-breaking, not advisory.
    invalid_annotation_target: ignore        # freezed/json noise on some setups
    missing_required_param: error
    missing_return: error
    todo: ignore
    # Promote common foot-guns to errors so the AI must fix them.
    avoid_dynamic_calls: error
    cast_nullable_to_non_nullable: error
    unawaited_futures: error
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/*.config.dart"
    - "lib/l10n/generated/**"
    - "build/**"

linter:
  rules:
    # Correctness
    always_use_package_imports: true
    avoid_print: true
    avoid_relative_lib_imports: true
    cancel_subscriptions: true
    close_sinks: true
    discarded_futures: true
    no_adjacent_strings_in_list: true
    test_types_in_equals: true
    throw_in_finally: true
    unawaited_futures: true
    unnecessary_statements: true

    # Async / error handling
    avoid_catches_without_on_clauses: true
    avoid_catching_errors: true
    only_throw_errors: true

    # Style & consistency (keeps generated diffs small and AI output uniform)
    prefer_single_quotes: true
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true
    prefer_final_locals: true
    prefer_final_in_for_each: true
    require_trailing_commas: true
    sort_constructors_first: true
    directives_ordering: true
    unnecessary_lambdas: true
    use_super_parameters: true

    # API design
    avoid_positional_boolean_parameters: true
    use_named_constants: true

    # Flutter-specific
    use_build_context_synchronously: true
    use_key_in_widget_constructors: true
    sized_box_for_whitespace: true
    avoid_unnecessary_containers: true